const header=document.querySelector(".header");window.addEventListener("scroll",()=>{if(window.scrollY>30)header?.classList.add("scrolled");else header?.classList.remove("scrolled")});const btn=document.querySelector(".menu-btn");const nav=document.querySelector(".nav");btn?.addEventListener("click",()=>nav?.classList.toggle("open"));document.querySelectorAll(".nav a").forEach(a=>a.addEventListener("click",()=>nav?.classList.remove("open")));const lb=document.querySelector(".lightbox");const lbImg=document.querySelector(".lightbox img");document.querySelectorAll(".masonry a").forEach(a=>a.addEventListener("click",e=>{e.preventDefault();if(lb&&lbImg){lbImg.src=a.href;lb.classList.add("open")}}));document.querySelector(".lightbox button")?.addEventListener("click",()=>lb?.classList.remove("open"));lb?.addEventListener("click",e=>{if(e.target===lb)lb.classList.remove("open")});